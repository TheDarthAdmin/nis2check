import { AppShell } from "@/components/app-shell";
import { findings } from "@/lib/demo";
import { Status } from "@/components/status";
export default function Compare() { return <AppShell><section className="hero"><div className="eyebrow">Run comparison</div><h1>Changes, not scores.</h1><p>Compare two evidence collections to focus on what has appeared, improved, or needs follow-up.</p></section><div className="compare"><div className="card"><b>17 August 2026</b><p className="muted">Previous completed run</p></div><div className="arrow">→</div><div className="card"><b>25 August 2026</b><p className="muted">Current completed run</p></div></div><table className="table"><thead><tr><th>Control</th><th>Previous</th><th>Current</th><th>Change</th></tr></thead><tbody>{findings.map(f=><tr key={f.id}><td>{f.id} — {f.title}</td><td><Status verdict={f.id==="C14"?"PASS":f.verdict}/></td><td><Status verdict={f.verdict}/></td><td>{f.changed}</td></tr>)}</tbody></table></AppShell>; }
