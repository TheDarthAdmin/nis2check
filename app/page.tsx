import { AppShell } from "@/components/app-shell";

export default async function Home({ searchParams }: { searchParams: Promise<{ error?: string }> }) {
  const { error } = await searchParams;
  return <AppShell><section className="hero home-hero"><div className="eyebrow">NIS2 technical evidence for Microsoft 365</div><h1>See the evidence. <span>Keep the judgement human.</span></h1><p>Nis2Check gives security and compliance teams a clear, tenant-scoped view of technically verifiable NIS2 evidence.</p><div className="hero-actions"><a className="primary-button" href="/api/auth/signin">Sign in with Microsoft</a><a className="secondary-button" href="https://github.com/TheDarthAdmin/nis2check">Explore the collector</a></div></section>{error ? <div className="error-note" role="alert">Sign-in could not be completed. Check the Entra application configuration and try again.</div> : null}<section className="trust-strip"><div><b>Read-only collection</b><span>Microsoft Graph GET requests only</span></div><div><b>Tenant isolated</b><span>Each organization has its own workspace</span></div><div><b>Evidence, not a score</b><span>No automated declaration of conformity</span></div></section><section className="home-grid"><article className="feature-card feature-card-primary"><div className="eyebrow">How it works</div><h2>Connect once, then see what changed.</h2><ol><li>Sign in with your Microsoft work account.</li><li>An administrator approves read-only access.</li><li>Review each control with its rationale and limitations.</li></ol></article><article className="feature-card"><div className="metric">28</div><h2>Verifiable controls</h2><p className="muted">One for every measure of NIS2 article 21(2) that a Microsoft 365 tenant can prove technically, each with the steps to remediate it.</p></article><article className="feature-card"><div className="metric">0</div><h2>Raw Graph responses retained</h2><p className="muted">Stored findings use summaries and pseudonymous object references.</p></article></section></AppShell>;
}
