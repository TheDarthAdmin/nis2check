import { AppShell } from "@/components/app-shell";

export default async function Home({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const { error } = await searchParams; return <AppShell><section className="hero"><div className="eyebrow">Microsoft 365 technical evidence</div><h1>Know what can be proved.</h1><p>Nis2Check reads your tenant and records technical evidence for NIS2 article 21. It does not produce a compliance score or a declaration of conformity.</p><p><a className="primary-button" href="/api/auth/signin">Sign in with Microsoft</a></p></section>{error ? <div className="error-note">Sign-in could not be completed. Please try again or ask an administrator to check the application configuration.</div> : null}<div className="note">The hosted view keeps raw Graph responses out of normal application screens. Stored findings use pseudonymous object IDs only.</div><section className="grid"><div className="card"><div className="metric">15</div><p className="muted">Technically verifiable controls</p></div><div className="card"><div className="metric">Read-only</div><p className="muted">The collector only sends GET requests to Microsoft Graph.</p></div><div className="card"><div className="metric">No score</div><p className="muted">Evidence is kept separate from audit judgement.</p></div></section></AppShell>; }
