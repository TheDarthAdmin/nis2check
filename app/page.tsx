import Link from "next/link";
import { AppShell } from "@/components/app-shell";
import { findings } from "@/lib/demo";
import { Status } from "@/components/status";

export default function Home() { return <AppShell><section className="hero"><div className="eyebrow">Microsoft 365 technical evidence</div><h1>Know what can be proved.</h1><p>Nis2Check reads your tenant and records technical evidence for NIS2 article 21. It does not produce a compliance score or a declaration of conformity.</p></section><div className="note">This is a representative hosted interface. Object names are resolved live from Microsoft Graph; stored findings contain pseudonymous object IDs only.</div><section className="grid"><div className="card"><div className="muted">Latest run</div><div className="metric">Complete</div><p className="muted">25 August 2026 · 10:42 UTC</p></div><div className="card"><div className="muted">Evidence retained</div><div className="metric">Off</div><p className="muted">Raw Graph responses are not stored.</p></div><div className="card"><div className="muted">Controls</div><div className="metric">15</div><p className="muted">Across authentication, devices and access.</p></div></section><h2>Latest findings</h2><table className="table"><thead><tr><th>Control</th><th>Domain</th><th>Verdict</th><th>Change</th></tr></thead><tbody>{findings.slice(0,5).map(f=><tr key={f.id}><td><Link href={`/findings/${f.id}`}><b>{f.id}</b> — {f.title}</Link></td><td>{f.domain}</td><td><Status verdict={f.verdict}/></td><td>{f.changed}</td></tr>)}</tbody></table><p className="muted">No aggregate score is shown: a percentage obscures the evidence an auditor needs.</p></AppShell>; }
