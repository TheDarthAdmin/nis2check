export function TenantOnboarding({ failed }: { failed?: boolean }) {
  return <section className="empty-state"><div className="eyebrow">Connect Microsoft 365</div><h1>Administrator approval is required.</h1><p>Nis2Check identified your Microsoft tenant. A tenant administrator must approve the read-only Microsoft Graph permissions before evidence can be collected.</p>{failed ? <p className="error-text">Approval was not completed. Sign in as a tenant administrator and try again.</p> : null}<p><a className="primary-button" href="/api/onboarding/admin-consent">Approve read-only access</a></p><p className="muted">Microsoft creates the Nis2Check Enterprise Application in your tenant during this approval. Nis2Check never receives a user’s Microsoft Graph token.</p></section>;
}
